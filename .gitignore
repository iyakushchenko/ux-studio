node_modules
dist
.DS_Store
*.local
.vite
scripts/playwright-out/
# Local Cursor config (MCP, etc.) — keep project rules tracked
.cursor/*
!.cursor/rules/
!.cursor/rules/**
scripts/inspect-*.mjs

# Local REC dumps (do not ship)
recordings/
