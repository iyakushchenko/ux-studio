# UX Studio — next steps (living board)

**Updated:** 2026-07-22  
**Owner:** Arch (agents execute; human PO accept/reject + assets only; **Pax** sim for bump/push)  
**Status authority:** This file is the **only mutable source of truth** for current priority, sequence, blockers, and completion. Forecasts, briefs, and feature registers may preserve historical context; when they disagree with this board, treat their status prose as historical. Evidence artifacts remain authoritative for the proof they record.  
**Team:** [TEAM.md](./TEAM.md) · Forecast: [PRODUCT_FORECAST.md](./PRODUCT_FORECAST.md) · Painpoints: [PAINPOINTS.md](./PAINPOINTS.md) · [ARCHITECTURE.md](./ARCHITECTURE.md) · [HYGIENE.md](./HYGIENE.md)  
**Refs:** [COMMAND_DOCTRINE.md](./COMMAND_DOCTRINE.md) · [CI_ACTIONS_BUDGET.md](./CI_ACTIONS_BUDGET.md) · [NAMING.md](./NAMING.md) · [../shell/URL.md](../shell/URL.md) · [../shell/CJM_RECORD_PLAY_EDIT.md](../shell/CJM_RECORD_PLAY_EDIT.md)

---

## Done definition — React page (erase-Legacy program)

A screen is **DONE** only when **all** are true:

1. **React-mounted** under UXDS + project chrome (`data-studio-react-screen=<screenId>`).
2. **Legacy child retired from view** with wire gates:
   - Prefer **deleting** the Legacy Frame child when safe (no CJM/playback regress).
   - Else hide via `data-studio-legacy-retired=<screenId>` + `display:none`; Legacy wire effects **must** early-return when React is mounted.
3. **No LEGACY growth** for the React path (screen CSS / UXDS / theme only).
4. **Honest residual** documented in the feature brief / audit (what Legacy still exists in DOM, what fidelity is Partial).
5. Uma audit **PROVEN** (or honest FAIL with blockers) + Quinn prove (tests + localhost).
6. **PAGE FINAL PASS hard-green** — [PAGE_FINAL_PASS.md](./PAGE_FINAL_PASS.md) + `check:page-final-pass` (Finn/Uma). **Required before the next migrated page may start.**

---

## NOW

0. [x] **Agent testing mid-flight QA shell (PP-10)** — `src/app/shell/agent-testing/`; R11 `:5173` mid-flight prove PASS 2026-07-20 (colors/names/timer/sitrep/alarm/cursor/scroll/timeline/START-END/dumps). Track: [PAINPOINTS.md](./PAINPOINTS.md).
0a. [~] **QA overlay trust / dual-role self-test (PP-13)** — pack expanded 2026-07-20 (pause/capture, interactive-only, Session origin, fail-freeze, presence, RTT, STEPPED/PLAYBACK, RESULT seal, Message withhold, stale-green, diag mirror). Self-test: [`SELF_TEST.md`](../../src/app/shell/agent-testing/SELF_TEST.md) · `__studioRunQaSelfTestSmoke()`. Keep eyes until residuals rare.
0b. [x] **Playback reliability / diag-first (PP-01…PP-08)** — 2026-07-20 wave: agentic Play→end + retreat + traditional Play **PASS** on `:5173`; PP-01/03/06/07 → **WATCH**. Keep PLAYBACK_DIAG; residuals stay eyes-on (not COMPLETE forever). See [PAINPOINTS.md](./PAINPOINTS.md).
0c. [x] **CJM Record/Play/Edit = guitar tabs** — doctrine [CJM_RECORD_PLAY_EDIT.md](../shell/CJM_RECORD_PLAY_EDIT.md); Play≡Step; Edit-by-user-story; Book Step2 already-selected → **other** 24/16:30; agentic prove `__studioRunAgenticFullPlayProve`.
0d. [x] **Traditional CJM smoothness** — fixed 2026-07-23 (Phase A of the erase-Legacy wave, commit `3909eb0`). Root cause: the page-land effect's forced `resetPrototypeScroll({ force: true })` re-asserted scroll ~560ms after landing even while a camera beat legitimately owned scroll (dwell+ease / reveal-into-view) on Book Step 3 / History / Details — the delayed re-assert snapped mid-flight eases back to 0, reading as a "scroll-reversal yank." Added a `yieldToActiveCameraWork` option (checks `isPlaybackScrollAnimating()` / `isCameraBeatDwellActive()`) gating only the page-land call site; other force-reset call sites (jump-to-start, go-home) stay unconditional. 4 new unit tests. **Re-proven live 2026-07-23** as part of Phase E's traditional `__studioRunFullPlayProve` run: `pass:true`, 10/10, `errors:[]`, crossing exactly the book-step-3/history/details route from the original PO dump — no scroll-reversal observed. Sitrep: [TRADITIONAL_CJM_UX_2026-07-21.md](../projects/boots-pharmacy/audits/TRADITIONAL_CJM_UX_2026-07-21.md) (historical symptom record).
0e. [x] **Chat bubble appear chop (PP-14 REOPEN)** — 2026-07-23 investigation closed. Live MCP re-prove (Step forward through q0→r3, `cjm=on`, real 60fps rAF sampler bypassing the diag's own 4-frame dump decimation) found **zero reproducible jump/chop** in current production — all bubbles monotonic transform + layout descent, `jumps=0 chops=0`. Root **self-test gap confirmed and closed**: `analyzeChatBubbleMotionSamples` only ever checked the isolated transform `y` for monotonic descent (`continuousY`); the composited **layout** position (transform + camera co-travel scroll) was only capped by `|ΔΔY|<=10` magnitude, never checked for sign-consistent (non-jittery) descent — a transform+scroll pair that individually ease smoothly but drift out of phase could oscillate within that 10px band and read self-test-green while looking choppy. Added `continuousLayoutY` + named `layout-continuous` check (`src/app/shell/agent-testing/chatBubbleMotionSelfTest.ts`), proven by a targeted unit test that fails on synthetic in-band wobble the old gate missed, and confirmed non-regressive against the live production bundle (`layout-continuous: 7/7 ok`). Not reproducible live ≠ invented-fixed: see [PAINPOINTS.md](./PAINPOINTS.md) PP-14 for full evidence + [LESSONS_LEARNED.md](./LESSONS_LEARNED.md) for the root-cause writeup. **Do not claim PROVEN from self-test alone** still stands as doctrine — this ship is proven by live diag/rAF trace, not the self-test's own green.
0f. [x] **Appointment History React migration** — **PAGE FINAL PASS HARD-GREEN** 2026-07-22 (`appointment-history`). Uma PROVEN · Quinn MCP 8/8 · densify gate · audit [FE_AUDIT_APPOINTMENT_HISTORY_PAGE_FINAL_PASS_2026-07-22.md](../projects/boots-pharmacy/audits/FE_AUDIT_APPOINTMENT_HISTORY_PAGE_FINAL_PASS_2026-07-22.md). **Details unblocked.**
0g. [x] **Appointment Details React migration** — **PAGE FINAL PASS HARD-GREEN** 2026-07-22 (`appointment-details`). Uma PROVEN · Quinn MCP 10/10 · densify 20/20 · audit [FE_AUDIT_APPOINTMENT_DETAILS_PAGE_FINAL_PASS_2026-07-22.md](../projects/boots-pharmacy/audits/FE_AUDIT_APPOINTMENT_DETAILS_PAGE_FINAL_PASS_2026-07-22.md).
0h. [~] **CX Conveyor Stage 1 — interactive identity contract** — 2026-07-24. Roadmap: [CX_CONVEYOR.md](./CX_CONVEYOR.md) (PO's "hand an agent a user story, it builds pages + good-enough controllers" ask, sequenced into 4 stages). Stage 1 shipped: `check:interactive-identity` gate (25 pre-existing gaps baselined, ratcheted not retrofitted) — stops PP-49-class REC-blind-control regressions at PR time. Open: uniqueness check (duplicate selector detection, PP-49's second bug), burn down the 25 baselined gaps, converge interaction-inventory + REC-capture on one "ready" definition.
1. [x] **PLP React migration** — DONE by erase-Legacy definition. Brief: [PLP_REACT.md](../projects/boots-pharmacy/features/PLP_REACT.md) · audit [FE_AUDIT_PLP_2026-07-19.md](../projects/boots-pharmacy/audits/FE_AUDIT_PLP_2026-07-19.md).
2. [x] **PLP Legacy parity restore** — register [PLP_LEGACY_PARITY_REGISTER.md](../projects/boots-pharmacy/features/PLP_LEGACY_PARITY_REGISTER.md); P0 gaps fixed (bg fill, hero shadow, listing wrapper, preloader, filter chips); audit PROVEN; patch `0.0.5`.
2b. [x] **PLP fidelity rage fix** — Advantage bar, heart optimistic hover/click, Book now commerce hover, tile border removed, Reset Filters icon+text; Uma checklist + team-check harden; patch bump.
2c. [x] **PLP preloader + checkbox hover (PO rage #2)** — real Legacy spinner overlay (hide tiles, in-band); mint checkbox hover; Uma loading/checkbox sign-off gates; patch bump.
2d. [x] **PLP invent/dupe rage #3** — one loader label (no count duplicate), height lock, Legacy empty-heart navy hover (no fuchsia invent); MCP matrix mandatory; prior PROVEN revoked; `0.0.8`.
2e. [x] **PLP PAGE FINAL PASS hard-green** — structure stamp + `check:page-final-pass` green; Quinn MCP `__studioRunMcpPageProbe({ screenId:"plp" })` full matrix PASS (overlay + scroll-into-view + landmarks); `mcpFinalPass: HARD-GREEN` @ tip `6358184` / v0.0.17. Audit: [FE_AUDIT_PLP_PAGE_FINAL_PASS_2026-07-19.md](../projects/boots-pharmacy/audits/FE_AUDIT_PLP_PAGE_FINAL_PASS_2026-07-19.md). **PDP unblocked.**
3. [x] **PDP React migration** — DONE by erase-Legacy definition (2026-07-19). Register [PDP_LEGACY_PARITY_REGISTER.md](../projects/boots-pharmacy/features/PDP_LEGACY_PARITY_REGISTER.md) · brief [PDP_REACT.md](../projects/boots-pharmacy/features/PDP_REACT.md) · React `screens/pdp/*` (L1–L20) · Uma [UMA_FIDELITY_PDP_2026-07-19.md](../projects/boots-pharmacy/audits/UMA_FIDELITY_PDP_2026-07-19.md) **PROVEN** · Quinn [FE_AUDIT_PDP_MCP_2026-07-19.md](../projects/boots-pharmacy/audits/FE_AUDIT_PDP_MCP_2026-07-19.md) PASS · `PARITY_PROVEN` pdp proven.
3e. [x] **PDP PAGE FINAL PASS hard-green** — structure `status=proven` + `check:page-final-pass` green; Quinn MCP `__studioRunMcpPageProbe({ screenId:"pdp", reload:false })` **23/23 PASS** ~27242ms on tip `f5f004f` / v0.0.38 (PromoMessageStrip + tip-stable cursor; after NEEDS-REPROVE demote); teardown clean stay `screen=pdp`; Arch restore `mcpFinalPass: HARD-GREEN` / `hardGreen: true` @ tip `53da33f`. Audit: [FE_AUDIT_PDP_PAGE_FINAL_PASS_2026-07-19.md](../projects/boots-pharmacy/audits/FE_AUDIT_PDP_PAGE_FINAL_PASS_2026-07-19.md).
4. [ ] **Versioning habit** — append notes on every user-visible ship; Pax decides patch; Ben executes ([VERSIONING.md](./VERSIONING.md)).

---

## NEXT (erase-Legacy sequence)

**Gate:** each page starts only after the previous is **PAGE FINAL PASS hard-green** ([PAGE_FINAL_PASS.md](./PAGE_FINAL_PASS.md)). Parallel callsigns + Knowledge used still required.

5. [x] **Site Pilot React migration** — **PAGE FINAL PASS HARD-GREEN** 2026-07-20 (`site-pilot`). Uma PROVEN · Quinn MCP 15/15 · audit [FE_AUDIT_SITE_PILOT_PAGE_FINAL_PASS_2026-07-20.md](../projects/boots-pharmacy/audits/FE_AUDIT_SITE_PILOT_PAGE_FINAL_PASS_2026-07-20.md). **Chat Final Pass unblocked.**
6. [x] **Chat React migration** — **PAGE FINAL PASS HARD-GREEN** 2026-07-20 (`chat`). Uma PROVEN · Quinn MCP 20/20 · audit [FE_AUDIT_CHAT_PAGE_FINAL_PASS_2026-07-20.md](../projects/boots-pharmacy/audits/FE_AUDIT_CHAT_PAGE_FINAL_PASS_2026-07-20.md). Shared `SitePilotComposer`. **History/Details unblocked.**
7. [x] **Appointment History** — **PAGE FINAL PASS HARD-GREEN** 2026-07-22 (`appointment-history`). Uma PROVEN · Quinn MCP 8/8 · audit [FE_AUDIT_APPOINTMENT_HISTORY_PAGE_FINAL_PASS_2026-07-22.md](../projects/boots-pharmacy/audits/FE_AUDIT_APPOINTMENT_HISTORY_PAGE_FINAL_PASS_2026-07-22.md). **Details unblocked.**
7b. [x] **Appointment Details** — **PAGE FINAL PASS HARD-GREEN** 2026-07-22 (`appointment-details`). Uma PROVEN · Quinn MCP 10/10 · audit [FE_AUDIT_APPOINTMENT_DETAILS_PAGE_FINAL_PASS_2026-07-22.md](../projects/boots-pharmacy/audits/FE_AUDIT_APPOINTMENT_DETAILS_PAGE_FINAL_PASS_2026-07-22.md).
7c. [x] **Header re-author (Legacy clone → hand-built UXDS React)** — 2026-07-23, live-MCP-verified. `setupHeader` no longer does `sourceHeader.cloneNode(true)` anywhere — `headerMount.tsx` now `createRoot`-renders a hand-authored `<Header>` (`src/projects/boots-pharmacy/chrome/Header.tsx` + `headerContent.ts`, same extraction shape as `Footer.tsx`/`footerContent.ts`: nav items, logo/chevron SVG paths, aux Search/Login glyphs, 5-item logged-in + 4-item guest account-flyout menu — all copied 1:1 from the prior hand-rolled `innerHTML` strings, none invented). `attachHealthServicesMegaMenu`/`attachFullScreenSearch` (unchanged kits) now attach to the React-rendered header DOM instead of the Legacy clone. Header shell + flyout CSS split to colocated `chrome/header.css` (file-hygiene ceiling — same pattern as `mega-menu-flyout.css`). Fixed a `flushSync`-inside-lifecycle-method warning (`setupHeader` runs inside a `useEffect`, so the initial-render `flushSync` was illegal there) by deferring `flushSync` + the mega-menu/FSC attach calls + DOM insertion into a `queueMicrotask`. **Live-verified via Chrome DevTools MCP on 2 screens (PLP, Book Step 1):** console clean (warning confirmed gone), Health Services mega menu opens/positions correctly, Full Screen Search opens with Boots-navy (`#012169`) link color intact, account flyout renders "Hi Sarah" + all 5 badged items. `npm test` 162/162 files (1008/1008 tests) green including `check:page-final-pass` (confirms the Header rewire caused zero source-contract regression across all 9 migrated `screenId`s), `npm run build` green. **Note:** Header is shared chrome, not a `screenId` in the `PAGE_FINAL_PASS.json` migration sequence — no separate manifest stamp applies; `check:page-final-pass` staying green *is* the relevant proof this lane didn't regress the gated screens. Formal Uma/Quinn audit doc not filed (spot-check only) — file one if PO wants a citable artifact before Phase E. `frame/index.tsx`'s Legacy header/footer *markup* is now unreferenced by Header/Footer chrome, but **Phase E readiness check (2026-07-23) found `frame/index.tsx` still NOT safe to delete**: `Frame219` (its sole export) is still rendered unconditionally as `<ProjectFrame />` in `BootsPharmacyProjectView.tsx:4831` — it's the live `.studio-viewport` page-shell substrate (nth-child columns) every screen mount, `dynamicCSS`, and `footerMount` still depend on, not just a Legacy-content source. Book Step 1-3 Legacy children are deleted only at the DOM level post-mount (Phase B); their JSX still lives in `frame/index.tsx` and still renders once per mount before removal. 6 other screens (plp/pdp/home/chat/appointment-history/details) still park-and-restore Legacy content from it on mount/unmount. Phase E is therefore a **substrate replacement** (stub 11 ordered page-shell columns to replace `<ProjectFrame />`, retire the 6 remaining park-mode mounts, drop the `ProjectFrame` export from `content.ts`), not a delete-after-grep. Exact blockers found: `BootsPharmacyProjectView.tsx:4831` (`<ProjectFrame />` render) + `:880` (import) + `content.ts:33` (re-export) + `footerMount.tsx:54-76` (mounts into Frame219 nth-child pages) + 6 park-mode screen mounts (`mountPlpScreen`/`mountPdpScreen`/`mountHomeScreen`/`mountChatScreen`/`mountAppointmentHistoryScreen`/`mountAppointmentDetailsScreen`) that still restore Legacy from Frame219 on unmount. Dead-safe-to-drop alongside: `src/imports/Frame1000007317/index.tsx` (unreferenced shim). Retro: [TEAM_RETRO_2026-07-22_HISTORY_DETAILS.md](./TEAM_RETRO_2026-07-22_HISTORY_DETAILS.md).
8. [x] **Delete Book Step 1–3 Legacy children** — 2026-07-23. `retireLegacyUnderPage` gained a `permanent: true` mode (no park entry, no restore path); Book Step 1/2/3 mounts now call it instead of park+restore, so Legacy header/breadcrumbs/body/footer nodes are deleted outright on first mount and can never come back. Also swept the 2 narrowly-dead CSS blocks (`.proto-confirm-open-appointment*`, `.proto-page-booster-checkbox*`) whose only possible producer was already-unreachable Legacy-path wire code and whose only target was the now-deleted `[data-name="body"]` subtree. `frame/index.tsx` JSX itself (and the larger "Legacy path only" wire dead-code family in `BootsPharmacyProjectView.tsx`) is untouched — out of scope (Header = Phase D lane 7c; LEGACY wire/CSS shrink = board #9). `npm test` 162/162 files green, `npm run build` green.
8b. [x] **Phase E — substrate replacement (`ProjectPageShell` swap) + `frame/index.tsx` deletion** — 2026-07-23, **HARD-GREEN, live-MCP re-proven.** New `src/projects/boots-pharmacy/chrome/ProjectPageShell.tsx` renders the exact 11 ordered, empty `div` columns Frame219 used to produce under `.studio-viewport > div` — verified byte-exact against **every** `.studio-viewport > div > div:nth-child(N)` reference across `BootsPharmacyProjectView.tsx` + `globals-chrome.css` + `vaccineList.ts` + `pdpRtb.ts` + `headerMount.tsx` + every `screens/*Contract.ts` (grep confirms N ranges 1–11 exactly, no gaps, no N>11). Swapped `<ProjectFrame />` → `<ProjectPageShell />` in `BootsPharmacyProjectView.tsx` (:4831 render, :880 import destructure dropped); dropped the `ProjectFrame` re-export from `content.ts` (:33). Converted the 6 remaining park-mode mounts (`mountPlpScreen`/`mountPdpScreen`/`mountHomeScreen`/`mountChatScreen`/`mountAppointmentHistoryScreen`/`mountAppointmentDetailsScreen`) to the Book Step 1-3 `permanent: true` precedent — no more `restoreLegacyUnderPage` calls. `footerMount.tsx` needed **zero changes**. **Live-MCP re-prove (2026-07-23, Chrome DevTools MCP on `:5173`):** all 9 screens (plp/pdp/site-pilot/chat/book-step-1/2/3/appointment-history/appointment-details) render correctly, console clean; Header mega-menu (Health Services, full column layout) + FullScreenSearch (full-width, scrim, Escape dismiss) both PASS on ≥2 screens; `__studioRunFullPlayProve({experience:"agentic"})` → `pass:true` 22/22; `__studioRunFullPlayProve({experience:"traditional"})` → `pass:true` 10/10, `errors:[]` — this route crosses book-step-3/history/details, the exact Phase A camera-yank risk class, with zero regression. One unrelated pre-existing console warning noted (framer-motion `ref`-prop warning in `ChatScreen.tsx`'s `ReplyFrame`, untouched by this diff — tracked separately, not a Phase E defect). **`frame/index.tsx` deleted** (0 live references beyond the dead `src/imports/Frame1000007317/index.tsx` shim, also deleted) **— but the containing `frame/` directory was NOT deleted**: it is a shared Figma-asset library (PNG images + `svg-p97rh8hlns.ts` path data) imported directly by `PlpScreen`/`PdpScreen`/`BookStep1-3`/`AvailabilityTool`/`ChatSitePilotBar`/`SocialIcons`/`healthServicesMegaMenuMount` — only the `index.tsx` Frame219 *component* was dead, not the folder. (First deletion pass wrongly `rm -rf`'d the whole folder, breaking 5 test files + build on `Cannot resolve .png` — caught immediately by re-running `npm test`, reverted via `git checkout`, redone as a precise single-file delete. See LESSONS_LEARNED 2026-07-23.) `npm test` 162/162 files (1008/1008 tests) green; `npm run build` green.
9. [~] **LEGACY retirement (by screen)** — shrink Legacy wire + `globals-screens` as React pages land. Concept `.proto-*` classes retire with their screen. **Scoping note (2026-07-23, from Phase B):** `BootsPharmacyProjectView.tsx` has ~10 `useEffect` blocks (~1500+ lines) commented "Legacy path only" and gated solely by `isBookStep{1,2,3}ReactMounted() → return` — since Book Step 1–3 mount unconditionally (no flag), these guards are always true and the effect bodies are permanently unreachable today. Prime first cut for this item. **Phase C — mechanical CSS shrink (2026-07-23, done for this pass):** removed 114 dead lines from `globals-screens.css` (3004→2891) — the `.proto-chosen-tile` / `.proto-chosen-store-card` / `.proto-chosen-checkbox` / `.proto-chosen-map` (bare, viewport, surface, dragging) / `.proto-page-map-pin` rule family under `.proto-chosen-slot` (only producer was `setupChosenPageMap` in `locationsMap.ts`, called solely from the dead Book Step 1 "chosen location" effect) plus the Book Step 1 (`nth-child(7)`) `.utility / cursor` hide rule (Legacy body there is now permanently deleted, no React producer). **Grep-proven dead** — zero remaining references outside the dead effect / the removed CSS. **Explicitly left in place (live, not dead):** `.proto-chosen-slot` base rule + `.proto-chosen-map-bg` (both still rendered by `BookStep1LocationScreen.tsx`), `.proto-confirm-advantage*` (rendered by `BookStep3ConfirmationScreen.tsx`), `component.book.appointment.progress` / `data-studio-book-step-back` rules + calendar-cell `data-studio-cal-*` rules (all still emitted by the live `BookAppointmentProgress` UXDS kit / `BookStep2DateTimeScreen.tsx` inside the same `nth-child(N)` host) — same family Phase B already verified live; not re-touched. The ~1500-line dead-`useEffect` JS family in `BootsPharmacyProjectView.tsx` itself is **still untouched** (not required to prove the CSS deletions above) — remains this item's next cut. Live `uxml play`/visual re-prove of Book Step 1–3 owed (no browser/MCP transport available this pass — static grep-proof only, per Phase B's own residual pattern).
10. [ ] **Engine monster splits** — on next touch of `App.tsx` / `useJourneyPlayback.ts`, extract by domain — not micro-files.
11. [ ] **Grow UXDS by page** — extract only on second use ([COMPONENT_LIBRARY.md](./COMPONENT_LIBRARY.md)).
12. [ ] **Residual fidelity (low)** — Book Steps 1–3 Legacy-only hexes with no bridge token yet. Do **not** invent aliases. PLP residuals: AI promo strip, View all, catalog depth (see register). Advantage bar restored.

---

## LATER

12a. [ ] **P0 future — Playback/REC/QA prove architecture refactor + single regression harness** — **not now** (PO 2026-07-22). Pain: monster token burn + scattered law (`completeJourneyPlay` + many prove contracts) so a one-line product flip (e.g. play-end stay-at-end) cascades across smokes/docs/asserts. Desired: one SSoT behavior module + thin prove API + fewer duplicated play-end at-start/at-end contracts. Track: [PAINPOINTS.md](./PAINPOINTS.md) PP-41 · forecast LATER. **Do not start until Arch/Pax open this wave.**
12b. [ ] **Hover-reveal REC capture (engine-wide) — backlog, not now** (PO 2026-07-23). Today: no `hover` event kind exists in the recording pipeline (`transport | touchpoint | screen | demo-click | director-script | beat-enter | wire-intent | studio | scroll | typed-text | dwell` — see [RECORDING.md](../shell/RECORDING.md)); hover-revealed panels (e.g. `MegaMenuFlyout`) unmount on close so a recorded click has no DOM target on replay. **Desired (do this, later):** first-class `hover-reveal` event kind through capture → compile → replay (`recordingTypes.ts` / `recordingCapture.ts` / `recordingCompile.ts` / `recordingReplay.ts` + PLAYBACK_DIAG kind) — an engine feature, not a one-off mega-menu patch. **Decision now:** leave the gap as-is (option A) — do **not** build the interim "keep mounted, hidden via CSS" workaround (option B) while this sits in backlog. PO decision: [PRODUCT_OWNER_BRIEF.md](./PRODUCT_OWNER_BRIEF.md) ledger 2026-07-23. **Do not start until Arch/Pax open this wave.**
13. [ ] **Second project rabbit** — after Boots book + URL + REC proven on Pages.
14. [ ] **Release / tag CI** — when versioning habit is stable and Actions budget allows.
15. [ ] **Broader CSS check ratchets** — more `scripts/check-*.mjs`, not more Playwright on every push.
16. [ ] **On-demand lean smoke** — keep `workflow_dispatch` / local `npm run smoke`; do **not** return auto smoke to default CI without a Director rewrite of this board.

---

## Done recently (context)

- [x] **PLP Legacy parity restore** — register + P0 fidelity; audit PROVEN; `v0.0.5`.
- [x] **PLP preloader + checkbox hover** — Legacy overlay scenario + mint hover; team loading gates; `v0.0.7`.
- [x] **PLP React migration** — erase-Legacy DONE; audit PROVEN; Legacy child 9 retired (delete deferred).
- [x] **Honest version chip + unstick agent overlay** — tip `6c5c911`.
- [x] **REC capture gaps (v3)** — beat-enter / scroll / typed-text capture+replay; compile still gaps scroll/typed.
- [x] **Post-agent clean slate** — hub home + strip modal; Quinn localhost PROVEN.
- [x] **Lean UX team OS + modal URL + sitrep z-index**.
- [x] **Recording compile→journeys (vertical)**.
- [x] **Version chip + felony gate**.
- [x] **Book Steps 1–3 React pilots** — Legacy retired via `data-studio-legacy-retired` (delete Legacy children deferred to end of erase-Legacy sequence).
- [x] **Domain CSS/attrs + hygiene + naming pack**.

---

## Hard locks (do not regress)

| Lock | Rule |
|------|------|
| **Erase-Legacy DONE** | React mount + Legacy retired + wire gates + no LEGACY growth + honest residual + **PAGE FINAL PASS hard-green** |
| **PAGE FINAL PASS** | **No new migrated page** until previous hard-green — stamp `PAGE_FINAL_PASS.json` + `check:page-final-pass` ([PAGE_FINAL_PASS.md](./PAGE_FINAL_PASS.md)); Arch enforces |
| **Sequence** | PLP → PDP → Site Pilot (`screenId=site-pilot`) → Chat → History/Details → delete Book Legacy children (each step gated by Final Pass) |
| **REC ⊗ CJM ⊗ AIR** | REC off when journey mode on; XOR; AIR locks both |
| **No LEGACY growth** | New React page styles → screen CSS / UXDS / theme only |
| **No new `.proto-*` / `data-proto-*`** | PANEL/chrome/attrs use `.studio-*` / `data-studio-*` |
| **Strict interface audit** | UI ship needs audit **PROVEN** before PO green-light |
| **CI budget** | No auto marathon Playwright on every push |
| **Post-push (R12)** | Optional `gh run list` peek; **no await** unless HARD-GREEN / release / PO prove |
| **Hygiene** | `check:hygiene` must stay green |
| **Felonies** | `check:felonies` + `check:version` + `check:page-final-pass` in `npm test` |
| **Naming** | [NAMING.md](./NAMING.md); folder = `screenId` |
| **Clean URL** | No sticky `?proof=*` |
| **Workspace** | `E:\UX\ux-studio` only |

---

## Related

- [PAINPOINTS.md](./PAINPOINTS.md)
- [PAGE_FINAL_PASS.md](./PAGE_FINAL_PASS.md)
- [PRODUCT_FORECAST.md](./PRODUCT_FORECAST.md)
- [ARCHITECTURE.md](./ARCHITECTURE.md)
- [HYGIENE.md](./HYGIENE.md)
- [LESSONS_LEARNED.md](./LESSONS_LEARNED.md)
- [POST_CHANGE_CHECKLIST.md](./POST_CHANGE_CHECKLIST.md)
- [PRODUCT_OWNER_BRIEF.md](./PRODUCT_OWNER_BRIEF.md)
- [ ] Introduce shell-level dark/light appearance modes after the neutral dark shell surfaces and token contract are stable; do not couple shell appearance to project themes.
